apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-renamer.fullname" . }}
  labels:
    {{- include "media-renamer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-renamer.selectorLabels" . | nindent 4 }}